# Testing

## Local gate

From a clean checkout:

```bash
make validate
```

This runs `./scripts/validate.sh` and should exit 0 with no network required.

## Scope

This fixture is disposable Phase-0 readiness scaffolding. It is not a production harness and does not certify third-party agents or models.
